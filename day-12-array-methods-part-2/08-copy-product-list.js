const products = ["Laptop", "Mouse", "Keyboard", "Monitor", "Printer"];

const requiredProducts = products.slice(0,3);

console.log(`Old list of products:
${products}`);

console.log();

console.log(`New list of products:
${requiredProducts}`);